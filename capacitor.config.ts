import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
    appId: 'io.multisourceplayer.multisourceplayer',
    appName: 'multi-source-player',
    webDir: 'dist',
}

export default config
